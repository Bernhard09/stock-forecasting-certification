1. Build virtual environment: python -m venv venv
2. Activate the venv in terminal: source venv/bin/activate (linux/macos)
3. install dependencies: pip install -r requirements.txt
4. run streamlit: streamlit run app.py